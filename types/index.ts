export enum AuthMode {
    LOGIN = "login",
    SIGNUP = "signup",
}

export enum UserRole {
    AGENT = "agent",
    STUDENT = "student",
}

export enum Gender {
    MALE = "male",
    FEMALE = "female",
    OTHER = "other",
}